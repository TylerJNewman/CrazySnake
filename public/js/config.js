(function() {

var Game = window.Game = window.Game || {};

Game.Config = {
  //these are not adjustable currently
  boardHeight: 30,
  boardWidth: 50,

  fps: 10,
  // speedFactor: 4,
  appleFrequency: 18
};

})();
